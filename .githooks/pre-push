#!/usr/bin/env sh
set -eu

if ! command -v cargo >/dev/null 2>&1; then
  echo "pre-push: cargo is required to run checks" >&2
  exit 1
fi

echo "pre-push: running cargo fmt --all -- --check"
cargo fmt --all -- --check

echo "pre-push: running cargo test --locked"
cargo test --locked
